stage('Build') {
    agent {
        label 'ubuntu-agent'
    }

    steps {
        sh '''
        cd maven-projekat
        mvn clean install
        '''
    }
}

stage('Ansible Deploy') {
    agent any

    steps {
        sh '''
        cd ansible
        ansible-playbook -i inventory playbook.yml
        '''
    }
}
